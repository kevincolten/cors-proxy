{
  "name": "cors-proxy",
  "version": "1.0.0",
  "description": "CORS proxy",
  "dependencies": {
    "claudia": "^5.4.2",
    "request": "^2.69.0"
  },
  "scripts": {
    "update": "claudia update --profile claudia",
    "create": "claudia create --profile claudia --region us-west-1 --handler handler.corsProxy --deploy-proxy-api",
    "start": "serverless offline"
  },
  "devDependencies": {
    "serverless": "^1.41.1",
    "serverless-offline": "^4.9.4"
  }
}
